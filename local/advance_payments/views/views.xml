<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="advance_payments.list">
      <field name="name">advance_payments list</field>
      <field name="model">advance_payments.advance_payments</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="advance_payments.action_window">
      <field name="name">advance_payments window</field>
      <field name="res_model">advance_payments.advance_payments</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="advance_payments.action_server">
      <field name="name">advance_payments server</field>
      <field name="model_id" ref="model_advance_payments_advance_payments"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="advance_payments" id="advance_payments.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="advance_payments.menu_1" parent="advance_payments.menu_root"/>
    <menuitem name="Menu 2" id="advance_payments.menu_2" parent="advance_payments.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="advance_payments.menu_1_list" parent="advance_payments.menu_1"
              action="advance_payments.action_window"/>
    <menuitem name="Server to list" id="advance_payments" parent="advance_payments.menu_2"
              action="advance_payments.action_server"/>
    -->
  </data>
</odoo>