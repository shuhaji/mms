<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="shift.list">
      <field name="name">shift list</field>
      <field name="model">shift.shift</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="shift.action_window">
      <field name="name">shift window</field>
      <field name="res_model">shift.shift</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="shift.action_server">
      <field name="name">shift server</field>
      <field name="model_id" ref="model_shift_shift"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="shift" id="shift.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="shift.menu_1" parent="shift.menu_root"/>
    <menuitem name="Menu 2" id="shift.menu_2" parent="shift.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="shift.menu_1_list" parent="shift.menu_1"
              action="shift.action_window"/>
    <menuitem name="Server to list" id="shift" parent="shift.menu_2"
              action="shift.action_server"/>
    -->
  </data>
</odoo>